services:
  joshuapunyanan-web:
    build:
      context: .
      dockerfile: Dockerfile

    restart: unless-stopped

    expose:
      - "3000"

    networks:
      - proxy-network

networks:
  proxy-network:
    external: true
